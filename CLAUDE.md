# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a general-purpose workspace (vibe coding project). No specific application or build system is configured yet.

## Permissions

- WebSearch is allowed via `.claude/settings.local.json`. Use it for research tasks and fact-checking.

## Repository State

Currently an empty Git repository with no source code, dependencies, or tests. Future work should add relevant tooling as needed.
